<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/md_theme_light_pink"/> <!-- Color del fondo, en este caso blanco -->
    <stroke
        android:width="2dp"
        android:color="#000000" /> <!-- Color y grosor del borde -->
    <corners android:radius="20dp" /> <!-- Radio de las esquinas redondeadas -->
    <padding
        android:left="2dp"
        android:top="2dp"
        android:right="2dp"
        android:bottom="2dp" /> <!-- Espacio interior del borde -->
</shape>
